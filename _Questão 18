#include <iostream>
using namespace std;

void incrementar(int* p) {
    (*p)++;
}

int main() {
    int x = 10;

    cout << "Antes: x = " << x << endl;
    incrementar(&x);
    cout << "Depois: x = " << x << endl;

    return 0;
}
